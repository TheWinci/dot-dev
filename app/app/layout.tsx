import { Metadata } from "next";
import { Fragment, PropsWithChildren } from "react";
import Navigation from "./navigation";

export const metadata: Metadata = {
  title: "winci.dev",
  description: "Generated by create next app",
};
import { NavigationConfig } from "./navigation"; // Import the missing NavigationConfig type

const navigationConfig: NavigationConfig[] = [
  { label: "COOKBOOK", path: "/app/cookbook" },
];

export default function Layout({ children }: PropsWithChildren) {
  return (
    <div className="h-full w-full bg-grid">
      <div className="relative flex h-full w-full">
        <Navigation header footer accent="sky-700" config={navigationConfig}/>
        <div className="flex w-full overflow-hidden">{children}</div>
      </div>
    </div>
  );
}
